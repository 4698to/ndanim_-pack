安装：
1、把文件夹覆盖到3ds Max2015安装文件夹；
2、点击设置栏的Customize -> 选择 Toolbars栏 -> 选择Action里的 S3_BatExp，拖到工具栏 -> 点击按钮启动。

使用说明：

1、勾选缩放或校验，必须选择绑点模型；

2、如果勾选缩放且缩放不等于1时，导出目录为"S3_export_"+缩放倍数；

3、如果勾选校验，则动作模型的基本节点结构必须和绑点模型的一致，才能通过，否则会提示校验不通过，且不会导出。

4、点击“暂停”按钮可以暂停导出，点击“继续”可以继续导出；点击停止则中止导出，需要点击“批量导出”才能重新开始。
